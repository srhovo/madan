# OS / editor
.DS_Store
Thumbs.db
*.swp
*~

# Node / build artifacts（本项目无构建，但保留以防万一）
node_modules/
*.log

# 注意：madan-<版本>.zip 必须留在仓库
# 更新包由 Cloudflare Pages 从仓库根目录直出，version.json 的 url 指向它；
# 一旦漏提交，已安装设备 OTA 下载会 404。因此这里刻意不忽略 *.zip。

# 临时/校验 hash 文件（误提交过，见历史）
*.sha256
*.sha1

# 测试运行产物：每跑一次 tests/run-all.sh 就会重新生成，无版本价值
.test-out/
report/

# 本地验证用的临时解包目录
_verify_pkg/

# 注意：src/ 与 tools/ 必须留在仓库（A4 起）
# src/chunks/ 是懒加载 Feature 的唯一可读源码，tools/ 负责把源码生成为
# index.html 里的内联字符串。它们不是「构建产物」，而是源头。
