# 码单器更新日志


## 8.3.36（发布流程自动化（A1+A2））

A1+A2 发布流程自动化 —— 新增 tools/release.sh 一键发布脚本、tools/set-version.js 版本号落点改写器、tests/release-selftest.sh 负向自检，以及 GitHub Actions 门禁。本版为纯工程改动：index.html 一行未动，用户侧功能与 8.3.35 完全一致。

A1 把「改版本号→同步内联→打包→算 checksum→写 version.json→追加 CHANGELOG→跑全套防线」串成一条命令，失败整体回滚。开发过程中负向自检（主动注入 8 类故障）实测出 release.sh 一处真实缺陷：bash 的 ERR trap 对显式 exit 1 不生效，导致所有失败路径都跳过回滚、文件停在半发布状态；已改为统一走 die() 出口，并新增断言锁死该行为。

A2 加 GitHub Actions 门禁，每次推送跑 11 套防线，把「发版前跑测试」从纪律变成机制 —— 历史上两次线上事故（8.3.24 版本号与包不同步、8.3.26~8.3.29 包内漏文件）都源于人忘了跑。同时把 run-all.sh 里「没有对应 zip 就静默跳过」改为可强制的 --require-package，堵住全项目唯一「出问题反而变绿」的洞。


## 8.3.35（优化清单 B1+B2：CSS 重复块去重 + 版本号单一真源常驻防线）

优化清单 B1+B2，零功能改动、零逻辑改动。B1（CSS 去重）：删除一处完全重复的响应式媒体查询块 —— 平板竖屏（768–1024px）「恢复单栏」规则整块出现了两遍，字节完全相同，保留带说明注释的那一份（覆盖 iPad Pro 12.9 竖屏 1024px 宽度），删掉前面无注释的早期副本，共减少 12 行 CSS。其余 128 个「多次定义」的选择器经逐条核对属于不同上下文/不同断点下的有意级联覆盖，不构成无效声明，一律保留。等价性用真实 Chromium 在 6 种视口下比对 58320 项计算样式（其中 3 种视口该媒体查询实际命中），零差异。B2（版本号单一真源）：核查 index.html 内 165 处 8.3.x 字样，剥离注释并排除标签属性后，真正参与代码的仅 <title> 与 APP_VERSION 两处（其余 163 处均为变更考古注释，按约定保留）；compareVersions 走 CURRENT_VERSION 间接引用，未发现游离字面量，故本项无需改代码。新增 tests/version-single-source.js 作为常驻防线，并交叉校验 <title>↔APP_VERSION↔version.json↔zip sha256，今后任何第二真源会在测试中立刻报红。测试防线 10 套 → 11 套，全部通过。

## 8.3.34（架构拆分第二步 A4+A5：源码外置 + 空 catch 注释化）

**本次零功能改动、零逻辑改动**：计价、提取、折扣、加价、记忆库、布局等全部逻辑与存储结构原样生效。app 可调用方法、state 键、feature 数量三项架构不变量与 8.3.33 完全一致（100 / 24 / 23）。

### A4 —— 把「不可读的产物」变成「可读的源码」

`dataPortability`（约 1117 行）与 `durationCalculator`（约 272 行）两个懒加载 Feature 的源码，此前只以 JSON 转义后的**单行字符串**存在于 `index.html` 的 `__INLINE_CHUNKS_RAW__` 里（该行 65,445 字符）。编辑器无法索引、无法 diff、无法 review —— 这是全项目最大的维护盲区。

本版做的是**源码与产物分离**，产物仍是单文件：

| 位置 | 角色 |
| --- | --- |
| `src/chunks/*.js` | **真正的源码**（人写这里，可索引 / 可 diff / 可 review） |
| `index.html` 的字符串 | **由脚本生成**（不手改，手改会被下次生成覆盖） |
| `tools/build-inline-chunks.js` | 生成器；`--check` 只校验 |

编码配方经**逐字节往返验证**：用 `src/chunks` 重新编码后与现存字符串完全相同，证明配方未漂移。

同时补 `tools/sync-root-scripts.js`，统一 `update-checker.js` / `analytics.js` 的内联同步——此前只有 `inline-integrity.js` 事后报错，缺正向同步手段。

### A5 —— 给空 catch 补上「为什么可以吞」

全量清点**9 处**空 catch（优化清单原记 6 处，实测多出 3 处），逐一补上就近注释。其中唯一有真实用户影响的是 `loadModeRatios()`：读取比例配置失败时会**静默丢失用户自定义比例**，已改为记 `appLogError`，便于用户反馈「自定义比例丢了」时定位根因。

其余 8 处均属「失败即降级、降级路径本身可用」，注释说明了降级去向（如设备号读取失败 → 逐级降级到内存兜底，仍返回可用 id）。

### 新增常驻防线（测试 7 套 → 10 套）

**`tests/arch-snapshot.js`（A3 遗留建议）** —— 用**运行时原型链内省**冻结三个架构不变量：`app` 可调用方法集合、`state` 键集合、`featureOrder`。

这道防线针对的是一类特殊问题：**静默失效**。8.3.33 修的 `bindToApp()` 隐式挂载和字符串数组动态派发都不报错、不崩溃，只是某个功能悄悄不工作了——静态扫描抓不住（本项目已实测会给出假绿灯）。已用注入式反例验证该测试确实能捕获并 exit 1。

### 过程中修正的一处失误

我最初直接在 `index.html` 里给 `update-checker.js` / `analytics.js` 的内联副本加注释，破坏了源文件与副本的**逐字节一致性**（被 `inline-integrity.js` 抓到 2 项失败）。正确做法是**改源文件再同步**。已改正，并把「同步」这个动作本身固化成工具，避免同类问题再犯。

### 验证

10 套防线全部通过：引擎 126、内联 chunk 一致性、内联完整性、根级脚本同步、架构边界快照、喂入链路 122、DOM 26、组合 27、五段式、变异捕捉 9/9、包自包含。

---

## 8.3.33（架构拆分第一步 A3：消除「隐式动态挂载」）

**本次零功能改动、零逻辑改动**：计价、提取、折扣、加价、记忆库、布局等全部逻辑与存储结构原样生效。

### 为什么要做

`OrderCalculator` 长期充当「编排队」，历史上为兼容旧调用点，把大量 Feature 的方法以 `this.xxx(...args) { return this.<feature>.xxx(...args); }` 的形式**平铺在自己身上**——133 个方法里 **77 个是这种单行转发**。

更严重的是 `RatioFeature.bindToApp()`：

```js
this.methodNames.forEach(name => { this.app[name] = this[name].bind(this); });
```

它在运行时把 12 个方法**动态挂到 `app` 上**。这正是工程边界明令禁止的「隐式动态挂载 / 全局隐藏状态」——静态读代码时根本无法发现这些方法是从哪来的，也让任何「这个方法是死是活」的分析都失效。

### 本版做了什么

1. **删除 59 个已无任何调用方的纯转发入口**
   实现**全部原样保留在其所属 Feature 内**，只是不再在 `app` 上镜像一份。
   （`app` 可调用方法 159 → 100；新增 0。）

2. **移除 `RatioFeature.bindToApp()` 的隐式动态挂载**
   改为显式路径：`ModeFlowFeature` 直接调 `this.app.ratioFeature.handleModeButtonClick(1|2)`。

3. **顺带修复一处被 1 暴露出来的既有隐患** ⚠️
   `dataPortability` 的 `refreshAfterImport()` 原本是这样写的：

   ```js
   ['applyLockedDataToUI', 'applyLayoutVisibility', 'updateSavedNamesList', 'updatePriceMemoryUI',
    'updateRatioCards', 'updateModeButtonText', 'updateModeIndicator', 'updatePeiPeiCount',
    'hideBossSuggestions']
     .forEach(method => { if (typeof this.app[method] === 'function') this.app[method](); });
   ```

   字符串数组 + `this.app[method]()` 动态派发（同属隐式动态挂载）。**用户导入备份数据后**的 7 项界面刷新依赖它——转发器一删，导入成功但界面不刷新，而且被静默吞掉不易察觉。现改为显式委托数组，导入后刷新链路恢复可靠。

4. **保留 `clearAll` 作为公共入口**
   它是外部可见的「清空全部」入口（测试脚本与外部脚本按此复位状态），不属于可删的冗余转发，以显式委托形式保留。

### 验证

- `app` 可调用方法 **159 → 100**（移除 59、新增 0）；**state 键 24/24、feature 数 23 与改动前完全一致**
- 59 项移除**逐一**给出四项证明：实现保留在所属 Feature ✅ · 全文无动态字符串访问 ✅ · 无 HTML 属性引用 ✅ · 无外部调用者 ✅
- 端到端行为验证：导入刷新、比例切换、模式按钮、模态注册、布局可见性、输入锁定、清空全部等 15 条路径全通过，**零静默异常**
- `tests/run-all.sh` 全量 7 套通过：引擎 126/126 · 喂入链路 122/122 · DOM 26/26 · 组合联动 27/27 · 五段式 33/33 + 8/8 + 30/30 + 13/13 · **变异捕捉 9/9** · OTA 包自包含性通过

## 8.3.32（补齐「缺失的信息」注释）

**本次零功能改动、零逻辑改动**：计价、提取、折扣、加价、记忆库、布局等全部逻辑与存储结构原样生效。

8.3.31 的审计结论里，把「写错的信息」修了，而把「缺失的信息」列为暂缓。本次把**暂缓的那部分全部补上**：

- **22 个类补上类级职责注释（已完成）**：`EnhancedNameExtractor`、`GiftMemoryEngine`、`ProjectExpressionEngine`、`PinyinKeyEngine`、全部 `Ui*` 组件（`UiModal`/`UiToast`/`UiConfirm`/`UiButton`/`UiCard`/`UiList`）、`NameCorrectionModal`、`FeatureEventScope`、`ServiceSuggestionFeature`、`PriceQuickPickFeature`、`PriceRuleEditorFeature`、`SurchargeFeature`、`GiftMemoryFeature`、`PriceMemoryFeature`、`DeviceScopeFeature`、`AutoPriceFeature`、`HistoryFeature`、`BossMemoryFeature`。
  > 修完之后，`index.html` 中 **56 个类 100% 带职责注释**，不再有「打开一个上千行的类却不知道它干什么」的情况。

- **两个内联 chunk 补上文件级 banner 注释（已完成）**：`dataPortability`、`durationCalculator`。注释写明了这个关键事实——它们是**被 JSON 转义后存于 `__INLINE_CHUNKS_RAW__` 单行字符串中的内联源码**，改动必须双向同步。这此前是仓库里最大的「注释真空区」（`dataPortability` 1025 行零注释）。

- **修正两处「注释张冠李戴」（已完成）**：
  - 原本贴在 `DurationCalculatorEngine` 上方的「集合持久化边界」注释 —— 文不对题，实为 `CollectionStoreBoundary` 的说明
  - 原本贴在 `CollectionStoreBoundary` 上方的「创建终极存储管理器」注释 —— 是 `UltimateStorageManager` 的残留
  - 两处均已改为与所注释类**真正对应**的说明

**验证**（改动仅涉注释，务必确保零行为变化）：

- 逐行核对：新增行**除内联模板字符串那一行外全部以 `//` 开头**；删除行仅 2 条旧注释
- 内联 chunk **正文逐字节一致**：解码后与修改前源码做全等比较，只有头部多了注释行
- `node --check` 全部 4 个内联脚本通过
- `tests/run-all.sh` 全量 7 套通过：引擎 126/126 · 喂入链路 122/122 · DOM 26/26 · 组合联动 27/27 · 五段式 33/33 + 8/8 + 30/30 + 13/13 · 变异捕捉 9/9 · OTA 包自包含性通过

## 8.3.31（注释与文档可维护性审计与订正）

**本次零功能改动、零逻辑改动**：计价、提取、折扣、加价、记忆库、布局等全部逻辑与存储结构原样生效。

做了一次全仓库的注释/文档可维护性审计，方法是用脚本做交叉比对（注释里提到的标识符是否真的存在、分隔线标题与实际类定义是否对得上、文档描述的套数/文件清单与仓库实际是否一致），共发现 5 类问题，本次订正其中**确定的 3 类**：

- **孤儿注释残骸（已清理）**：`index.html` 里曾有**连续 9 个「模块分隔线」标题**——集合持久化边界 / 8.2 价格规则数据模型 / 段位解析 / 跨类型冲突检测 / 多项目表达式解析 / 结构化项目与结算 / 历史记录存储边界 / 老板目录边界 / 单价记忆库。但它们描述的模块**早已定义在更靠前的行**（L5717~L8224），这串标题实际紧跟着的只有 `PriceLibraryStore` 一个类。这是历史上模块搬移留下的注释残骸，会让维护者按注释去找代码却找到完全无关的位置。已删除这 **30 行**冗余标题，仅保留与 `PriceLibraryStore` 对应的那一个。**纯删除，未改动任何代码行。**

- **README 订正（已修正）**：
  - 测试防线由「**6 套**」更正为「**7 套**」（两处），与实际一致
  - 结构清单**补齐 4 个发布专项测试脚本**（`check-package-selfcontained.py`、`ota-loop-guard.js`、`ota-e2e.js`、`inline-integrity.js`）
  - 标注 `update-checker.js` / `analytics.js` **自 8.3.30 起已内联进 `index.html`**，避免维护者去改外部文件却对产品无效
  - 新增「改了这两个 .js 之后必须同步内联副本」的醒目提示与校验命令

- **内联块补「维护须知」（已补充）**：`index.html` 的内联脚本块上方写明——这两个 `script` 块是仓库根目录两个 `.js` 源文件的**逐字副本**，改动需**双向同步**，否则打包出去的仍是旧逻辑；并给出校验命令。

- **顺带规避一个工具解析隐患**:HTML 注释里不再出现裸 `<script` 字样。审计中发现，按标签切片的测试脚本会把 HTML 注释里的 `<script` 误当作脚本起始，导致切片错位、报出与代码质量无关的假错误。

**审计中确认无问题的项**（一并记录，供后续参考）：

- 注释中引用的标识符（如 `mergeLayoutTemplates`、`refreshAfterImport`、`notifyAppReady`）**均准确**，无指向已删除代码的失效引用
- **无注释掉的死代码**（仅 1 处是有意保留的「每小时节流」配置示例）
- DOM id 相关注释与实际 `<input id="...">` **完全对应**
- `CHANGELOG` 与 `version.json` 的版本信息一致

**未在本次处理的两项**（属「缺失的信息」而非「写错的信息」，改动会触及 `index.html` 主体，留待后续按需处理）——**已在 8.3.32 全部补上**：

- 34 个顶层类中，**17 个没有类级职责注释**，且恰好包含行数最多的几个（`PriceMemoryFeature` 2105 行、`PriceRuleEditorFeature` 708 行 / 注释率 0.1%）
- 内联 chunk 中的 `DataPortabilityFeature`（**1025 行代码、零注释**）与 `DurationCalculatorFeature`（258 行 / 1 条注释）——这些代码藏在 `__INLINE_CHUNKS_RAW__` 那个 64KB 单行字符串里，是注释真空区

**验证**：

- `node --check` 全部 4 个内联脚本通过
- 删除前后 **diff 只有删除、没有其他变更**
- `tests/run-all.sh` 全量 7 套通过：引擎 126/126 · 喂入链路 122/122 · DOM 26/26 · 组合联动 27/27 · 五段式 33/33 + 8/8 + 30/30 + 13/13 · 变异捕捉 9/9 · OTA 包自包含性通过

## 8.3.30（修复安卓端「每隔一小会儿就自动重新加载」的循环重载）

> **现象**：只有安卓 APK（走 OTA 热更新的那个壳）会出现——App 用着用着就自己重新加载一次，靠「弹欢迎」之类的界面动作能明显看出来。**本地数据完好无损**。浏览器直接打开 index.html 没有这个问题。
> **首次间隔约 30 秒，之后 5 秒 → 30 秒 → 4 秒 → 7 秒，不规则**。
>
> **根因（在发布产物，不在业务代码）**：从 **8.3.26 起，OTA 更新包只打包了 `index.html` 一个文件**，而 `index.html` 里仍以 `<script src>` 引用 `update-checker.js` 与 `analytics.js`——**这两个文件没有进包**（8.3.24/8.3.25 的包还是 3 个文件，8.3.26 起变成 1 个）。
>
> 缺了 `update-checker.js`，后果是链式的：
> 1. 该脚本的第一件事是调用 `Updater.notifyAppReady()`——语义是「这一版资源包启动正常，请放行」；
> 2. 脚本 404 不执行 → `notifyAppReady()` 永远不被调用；
> 3. 原生层等满 `appReadyTimeout`（默认约 10 秒）→ 判定这一版包**不健康**（视为白屏/崩溃）→ **自动回退到旧包**；
> 4. 回到旧包后本机 `APP_VERSION` 仍是旧值 → 下次启动又判定「有新版本」；
> 5. → 下载 → 切换 → 回退 → 再判定 ……**无限循环**。
>
> 时间特征也吻合：间隔 = 下载 306KB 的耗时 + 切换 + 超时回退，其中网速波动占主导，所以**天然不规则**（若是纯定时器死循环，会是稳定的 2.5 秒）。

- **修复一：单文件彻底自包含（根治）**。把 `update-checker.js` 与 `analytics.js` 的内容**内联**进 `index.html`，删掉两个 `<script src>` 引用。内联后 `index.html` 的外部脚本引用数为 **0**，今后无论打包流程怎么改、只打 `index.html` 一个文件都不会再缺东西。
- **修复二：加「同一版本只尝试一次」的持久化闸门（止血）**。用 `localStorage` 的一个标记 key（`pw_ultimate_updateAttemptedVersion`）记住本机已经尝试过的最高版本号：同一版本**成功或不成功地只尝试一次**。即使将来还有别的因素导致切换不生效，也最多尝试一次就安静停在当前版本，不会无限重载。**更高版本仍能正常更新**，闸门不会误伤正常升级。
- **修复三：修掉一处危险兜底**。原先读不到本机版本号时会退化成 `'0.0.0'`（最小版本），于是**任何**云端版本都被判为「有新版本」。现在读不到就**放弃本次更新检查**——宁可停在当前版本，也不发车。`notifyAppReady()` 则不受影响，**任何路径下都会先被调用**（否则又会触发原生层误判回退）。
- **新增发布防线**：`tests/check-package-selfcontained.py` —— 检查包内 `index.html` 是否引用了包外不存在的资源。已接入 `tests/run-all.sh` 作为第 7 套。用 8.3.29 的旧包跑它会**直接点名** `update-checker.js` / `analytics.js` 缺失，用新包则通过。
- **专属回归用例**：`tests/ota-loop-guard.js` —— 在模拟 Capacitor 环境里跑**真实的** `update-checker.js`。对照实验：旧逻辑在「本机版本卡住」时 5 次启动**下载 5 次**（即无限重载）；新逻辑只下载 **1 次**。
- **验证（全部在真实产品上跑，非 mock）**：
  - **`tests/run-all.sh` 全量 7 套通过**：引擎 126/126 · 喂入链路 122/122 · DOM 26/26 · 组合联动 27/27 · 五段式 33/33 + 8/8 + 30/30 + 13/13 · 变异捕捉 9/9 · OTA 包自包含性通过。
  - **包内自包含性**：从 `madan-8.3.30.zip` 解压后，`index.html` 的外部 `<script src>` 引用数为 **0**，版本号 `8.3.30`。
  - **真实页面加载验证**：用完整 `index.html` 在模拟原生壳内加载，`notifyAppReady` 被调用、读到版本号 `8.3.30`（非空、非 `0.0.0`）、**零未捕获异常**。
  - **业务零改动**：计价、提取、折扣、加价、记忆库、布局等全部逻辑与存储结构原样生效。

## 8.3.29（纯内部重构：拆出 NameTextUtils 纯函数类 + 清理空 script 标签）

**本次零功能改动**：计价、提取、折扣、加价、记忆库、布局等全部逻辑与存储结构原样生效。

- **拆分巨型类**：`EnhancedNameExtractor` 由 **1038 行降至 961 行**。把其中 12 个**无状态纯函数**抽到新类 `NameTextUtils`（114 行）：
  `normalizeLearningText`、`getLearningKey`、`normalizeExtractText`、`compactExtractName`、`uniqueExtractNames`、`stripExtractPlatformPrefix`、`stripExtractQuotes`、`normalizeOutputName`、`cleanName`、`isValidName`、`isChineseName`、`isReasonableName`。
- **为什么是这 12 个**：逐个核查过它们的 `this.X` 引用，**全部落在这 12 个方法之内**，对实例状态（`this.data` / `this.config` 等）零依赖。因此抽离后依赖是**单向、无环**的；早先曾计划按「数据 / 算法 / 学习」三分，实测出 44 条跨块引用，已否决。
- **对外接口完全不变**：原类保留 12 个**同名转发方法**，签名与返回值逐字一致，任何外部调用方都不需要改动。
- **删除 100% 确定的冗余**：`index.html` 里有一个 `<script>…</script>`，其内部**只有一行注释、没有任何可执行语句**——每次加载都被 JS 解析器空跑一趟。改为 HTML 注释后语义不变。行首统计的真实 HTML `<script>` 标签由 5 个减为 4 个，其中内联脚本由 3 个减为 2 个。
- **顺带修掉测试脚本的隐性耦合**：「五段式全链路」脚本原先**写死** `html.indexOf('class EnhancedNameExtractor')` 作为切片起点。产品一旦把该类的方法抽到**定义在其之前**的新类里，切片就会漏掉新类 → 转发方法抛 `ReferenceError` → 异常被上层 `try` 吞掉 → 表现为「提取结果全为空」。本次正是踩中此坑（一度显示 0/8 的假回归）。现改为**依赖驱动**取起点：按括号配平取类体，扫描类体里 `Xxx.` 形式引用的外部类，再把这些类定义一并纳入切片（支持多跳传递依赖）。今后再做同类拆分不会再误报。
- **验证（全部在真实产品上跑，非 mock）**：
  - **行为指纹 sha256 完全一致**：`ea5e09a7…` 重构前 == 重构后。指纹覆盖 59 个方法，并用 `tests/_materials.json` 里的**真实聊天记录素材**驱动 `pat1`/`pat2`/`at`/`chain`，提取结果 **66 / 65 / 56 / 31 项逐字比对**（共 220 个真实名字）。
  - **`node --check`** 全部内联脚本通过。
  - **`tests/run-all.sh` 全量 6 套通过**：引擎 126/126 · 喂入链路 122/122 · DOM 26/26 · 组合联动 27/27 · 五段式 8/8 + 13/13 · **变异捕捉 9/9**。

## 8.3.28（修复加价候选的转义静默降级隐患 + 清理失效注释）

- **修复的问题**：`SurchargeFeature` 渲染加价候选时，转义函数写作 `this.app.escapeHtml?.bind(this.app) || (value => String(value ?? ''))`。那个**兜底分支不做任何转义**，而它转义的正是**加价规则名称与触发关键词**——都是用户自己输入的文本。一旦 `this.app.escapeHtml` 因任何原因被移除或改名，转义会**悄悄退化**为「原样拼字符串」，让带引号或尖括号的规则名直接进入 `title` 与 DOM。
- **根因**：防御性写法用了可选链 `?.` 配静默兜底函数。这种写法的本意是「取不到就不转义也别报错」，但转义是**安全相关**操作，不该有静默降级路径。
- **实现方式**：改为直接引用确定存在的 `AppTextUtils.escapeHtml`（顶层 `Object.freeze` 冻结对象、纯函数，不依赖 `this`）。去掉可选链与静默兜底后，万一将来真出问题会**显式报错**，而不是无声放行。
- **注释清理**（3 处与代码对不上的旧注释）：
  - `OrderEngine` 处的说明原先误挂为「提取修正弹窗」（复制残留），现补上它「纯静态工具类」的真实职责，并标明与 `OrderCalculator` 的区别（两者名字相似但职责完全不同，是最易迷路处）
  - `DialogManager` 上方三组已失效的旧模块边界注释（「增强名字提取器」「提取服务」「提取修正弹窗」）一并清理
  - `OrderCalculator` 补充架构地位说明，与 `OrderEngine` 形成对称区分
- **验证**：
  - 危险字符转义 6/6 通过（标签注入 / 双引号属性闭合 / 单引号属性 / 图片事件 / 正常文本 / 混合字符）
  - 三条转义路径（`uiRender` / `app` / `appShellRenderer`）全部正常
  - 全量回归：dom-full 26/26、combo 24/24、verify-823 49/49、五段式 33/33 + 8/8 + 30/30 + 13/13，运行时错误 0
  - 真机验证通过
- **零功能改动**：计价、提取、折扣、加价、记忆库、布局等全部逻辑与存储结构原样生效。

## 8.3.27（改完时长总价即时刷新，不必再切「局数/小时」）
- **修复的问题**：填好「1h + 未保存项目 + 自定义单价 25」后去**改时长**，总价不跟着刷新，界面只留一句「⚠ 第1项"××"当前价格库中没有匹配规则（可填写自定义单价以结算）」，**必须点一次「局数/小时」按钮才能把总价救回来**。
- **根因**：改时长时服务类型框往往要经过一次**清空**（全选重打、退格删空都会），而清空被当作「本单结束」，逐项单价连同总价一起作废；切按钮恰好会触发一次重算，于是表现为「切一下就好」。
- **实现方式（把「清空」拆成两种语义）**：
  1. **同话题改单** —— 清空后重新输入的是**同一个项目、只是改了时长**（`1h技术` → `3h技术`），视为仍在编辑这一单：单价自动套回，总价随新时长立即刷新（`3h × 3局 × 25 = 225`）。
  2. **换话题开新单** —— 清空后输入的是**别的项目名**，视为开新单，旧单价一律不套用，避免上一单的价串到下一单。
  3. 判定依据是「话题指纹」`describeExpressionTopic()`：抽取表达式里各项目的**归一化项目名序列**（刻意不含时长，因为改时长正是要放行的情况），清空那一刻先把旧指纹记下，随后输入与之比对。
  4. **原地改名（不经过清空）不受影响** —— 单价照常保留，符合「只保留单价、不绑定名称」的既定规则。
- **同时修掉的显示瑕疵**：清空瞬间把**总价与时长当场归零**，不再残留旧数字挂在界面上让人误以为没清干净（此前是草稿快照里的旧项目把总价又算了回来）。
- **边界处理**：
  - 清空后**就此停手**（不再输入）→ 单价立即失效，不会串到下一单。
  - 用户**主动在单价框里删空** → 仍视为撤销定价，总价清空。
  - 撤销后**重新填价** → 立即恢复结算（`3h + 40/局 = 360`）。
  - 切「局数/小时」后改时长 → 单价跟着单位走（小时模式下 `3h × 25 = 75`，切回局数 `225`）。
- **回归**：内联语法 3/3、五段式 33/33·3 块·8/8·30/30·13/13、dom-full 26/26、combo 24/24、verify-823 49/49、审计 44/44，全部通过；8.3.26 的切换单位与混用提示用例同步回归通过。
- **零破坏**：库价匹配、记忆库复用、混用单位提示、礼物模式、折数/加价等原有交互与存储结构全部保持原样。

## 8.3.26（修复局数模式总价算错 + 切换单位失灵 + 混用单位提示）
- **修复的问题**：在服务类型框输入「1h + 未保存项目名」（如 `1h新项目`）、并填自定义单价 25 / 选「局数」时，**总价一直显示 25，正确应为 75（3×25）**；且来回点「局数 / 小时」按钮**总价纹丝不动**，看上去像按钮失灵。
- **根因（两层，缺一不可）**：
  1. **结算单位只听库、不听按钮**。整串表达式的结算单位由价格库匹配推导（`ProjectExpressionEngine.resolve` 里 `quantityMode` 只取库匹配结果）。`1h新项目` 在库里不存在 → 匹配失败 → 兜底取到候选首位的 `hour` → 按 `1×25=25` 结算。正确语义应是「1 小时 = 3 局」，按用户所选的「局数」口径得 `3×25=75`。
  2. **切按钮不触发重算**。`setMode()` 只做样式切换 + `syncUI()`，从不重新解析表达式；更糟的是，切按钮会连带 `emitPriceInputChange → handleAutoPriceManualChange`，而后者读到单价框已被系统清空，就把该项的逐项定价**当成「用户撤销定价」删掉**——即使触发重算也已无价可用。
- **实现方式（明确规则边界，未重写引擎）**：
  1. `ProjectExpressionEngine.resolve` 新增 `preferredMode` 入参。**手输单价的项目**按 `preferredMode`（用户在按钮上的选择）结算；**命中价格库的项目**仍按库推导模式结算——**库价本身带结算单位语义，不受按钮影响**（本版与用户确认后的规则）。
  2. `setMode()` 在单位真正发生变化且处于表达式态时，取消待执行的同步并立即按新单位重算一次，切按钮即刻见效。
  3. 引入「**用户编辑标记**」`_expressionPriceUserEdited`：只有单价框的 `input` 事件（用户真的在敲字/删空）才允许撤销逐项定价；切按钮、复用记忆库价等**系统行为**清空单价框不再被误判为撤销。新增 `noteExpressionPriceEditedByUser()`，并在 `clearExpressionManualPrices()` 中一并复位该标记。
- **新增：混用结算单位的一行说明**：同一串表达式里若**库价项与手输项采用了不同单位**，会在「项目小计」下方多显示一行说明，例如：
  > 项目小计：80 + 150｜累计230
  > 王者荣耀按小时结算 · 神秘服务按局数结算

  文案由 `buildMixedModeNotice()` 生成，**仅在确实出现两种单位时返回**；上下单位一致的串返回空串、不显示任何提示，不产生噪音。服务类型框清空 / 表达式态归零时一并清除，不残留到下一单。
- **边界处理**：
  - **切按钮保留定价**：`A. 1h手输项 + 25/局` → 局数 75 ↔ 小时 25 ↔ 切回 75，双向可逆。
  - **记忆库复用同样可切**：`1h库项 42/局` → 126 ↔ 42 ↔ 126。
  - **用户主动删空单价**：仍会正确撤销锚定（总价随之清空），与「系统清空」区分明确。
  - **用户改单价**：锚定实时更新；多项目逐项定价 110 ↔ 36.667 双向可逆。
  - **提示不串单 / 不多余**：单一单位无提示；清空服务类型后提示与总价一并清空。
- **回归**：内联语法 3/3、五段式 33/33·3 块·8/8·30/30·13/13、dom-full 26/26、combo 24/24、verify-823 49/49、审计 44/44，全部通过。
- **零破坏**：库价匹配、记忆库复用、礼物模式、折数/加价等原有交互与存储结构全部保持原样；8.3.25 的「未保存项目 + 手输单价」能力完整保留。

## 8.3.25（未保存服务项目 + 手输单价：自动算总价并沉淀记忆库）
- **核心改进**：服务类型里写**未保存的服务项目**（如「1h神秘项目」「2局新项目」）时，配合「自定义单价」输入框即可**自动算出总价**，并把单价**沉淀进单价记忆库**，下次输入同名项目直接带价复用。
- **修复的问题**：此前整串表达式（如 `1h神秘项目+2局新项目`）只走正式价格库逐项匹配，**任一项目不在库里就整串否决、总价为空**；只有「已保存项目」能享受联动带价，「未保存项目」完全无法自动计价。
- **实现方式（复用现有规则，未重写引擎）**：
  1. `ProjectExpressionEngine.resolve` 新增 `manualPrices` 入参——按**项目序号**索引的手输单价数组，与单项目草稿路径 `resolveCurrentDraft` 的优先级完全一致：**手输优先覆盖库价，未填项继续走库价兜底**。
  2. `ProjectSettlementEngine.createProject` 原生支持的 `priceSource: 'manualOverride'` 被真正用起来，未命中库的项目也能凭手输价建立结算项目，账单上仍显示用户写的原始项目名。
  3. **静默自动归位**（用户选定方案）：服务类型框每输入一次，就把「当前正在编辑的项目序号」记为游标；随后填的自定义单价即归到该项目。多项目时各填各的价，不抢焦点、不打断录入节奏。
  4. `autoWritePriceMemory` 放宽 `existingOnly` 限制——**手输单价（manualOverride）即使属于多项目/整串表达式也允许入库**，这是「未保存项目能沉淀」的关键。仅在点「生成并复制订单」时写入，且**只写记忆库、不写正式价格库**。
- **边界处理（本版重点打磨）**：
  - **打字中间态**：`1h甲服务+` / `1h甲服务+2`（数字已敲、单位未敲）时，解析器会把残缺的 `+`、`+2` 并入前一项服务名。已按「剥离首尾分隔符 + 剥离结尾未成形数字片段」归一化后比较，**追加项目不会丢失已填的逐项单价**。
  - **整体改写 vs 前缀追加**：新串与旧串前缀不一致时判定为整体改写，锚定作废；仅在前缀一致（追加）时保留。
  - **服务类型框清空**：视为本单结束，立即作废逐项定价锚定，**不会串到下一单**。
- **回归**：内联语法 3/3、五段式 33/33·3 块·8/8·30/30·13/13、dom-full 26/26、combo 24/24、审计 44/44、verify-823 49/49，全部通过；新场景实测：未保存单项目手输 30 ✅、混合多项目 80+40×2=160 ✅、手输覆盖库价 50 ✅、三项逐项录入 30→50→55 ✅、记忆库闭环 42+18×2=78 ✅、生成订单前记忆库为空/后写入 ✅。
- **零破坏**：已保存项目联动、库价匹配、礼物模式、折数/加价等原有交互与存储结构全部保持原样。

## 8.3.24（服务类型提示更新 + 修正版本号错位导致的无限更新）
- **服务类型提示更新**：「服务类型」输入框的示例提示由「例如：游戏陪玩、语音聊天」改为 **「例如：1h+2局星耀包c+1h23min鹅鸭杀」**，更贴合实际录单时的混合写法（时长 + 局数 + 段位/包c 备注 + 服务项目）。
- **修复：安卓端无限自动更新循环（版本号错位）**
  - 现象：安卓 App 每次启动都重新下载更新包，陷入无限更新。
  - 原因：`version.json` 的 `version` 被单独改为 `8.3.24`，但 `madan-8.3.23.zip` 包内 `index.html` 的 `APP_VERSION` 仍是 `8.3.23`，二者出现**永久差值**。`update-checker.js` 判断"是否需要更新"用的是 `version.json` 的版本号，而判断"更新是否生效"用的是**包内** `APP_VERSION`——于是每次启动都判定"8.3.24 > 8.3.23，有新版本"，下载同一个包、装完版本号还是 8.3.23，下次启动再判定一次，循环往复。
  - 修复：将**代码版本与更新清单统一升至 `8.3.24`**——`index.html` 的 `APP_VERSION` 与 `<title>` 升至 8.3.24，重新打包 `madan-8.3.24.zip`，`version.json` 的 `version` / `url` / `checksum` 同步指向新包，二者严格对齐，循环终止。
  - **经验沉淀**：`version.json` 只是"更新清单"，本身不含代码；**发布新版本必须「改版本号（含包内 APP_VERSION）+ 重新打包 zip + 同步 version.json」三件套齐做**，只改其中任一环都会导致版本号与实际包内容不一致，进而触发上述循环。
- **本版同时包含 8.3.23 的内容**：头部「DIY布局」按钮升级为「图标 + 布局」的直接切换键；导入导出中移除「DIY布局」与「DIY布局模板」两项。安装 8.3.24 即同时获得这两项改进。

## 8.3.23（布局按钮改为直接切换 + 导入导出移除 DIY 选项 + 服务类型提示更新）

> **修复：安卓端反复自动更新的死循环**
>
> 现象：安卓 App 每次启动都重新下载更新包，陷入无限更新。
>
> 原因：`version.json` 的 `version` 被单独改为 `8.3.24`，但 `madan-8.3.23.zip` 包内 `index.html` 的 `APP_VERSION` 仍是 `8.3.23`，二者出现**永久差值**。`update-checker.js` 判断"是否需要更新"用的是 `version.json` 的版本号，而判断"更新是否生效"用的是**包内** `APP_VERSION`——于是每次启动都判定"8.3.24 > 8.3.23，有新版本"，下载同一个包、装完版本号还是 8.3.23，下次启动再判定一次，循环往复。
>
> 修复：将 `version.json` 的 `version` 与包内 `APP_VERSION` **统一为 8.3.23**，并重新打包 `madan-8.3.23.zip` 使二者严格对齐，同时同步新的 `checksum`。已在 8.3.23 的设备无需再下载，循环即刻停止。
>
> **经验沉淀**：`version.json` 只是"更新清单"，本身不含代码；**发布新版本必须「改版本号 + 重新打包 zip + 同步 version.json」三件套齐做**，只改其中任一环都会导致版本号与实际包内容不一致，进而触发上述循环。

- **服务类型提示更新**：「服务类型」输入框的示例提示由「例如：游戏陪玩、语音聊天」改为 **「例如：1h+2局星耀包c+1h23min鹅鸭杀」**，更贴合实际录单时的混合写法（时长 + 局数 + 段位/包c 备注 + 服务项目）。
- **布局按钮改造**：头部的「DIY布局」按钮文字改为 **「图标 + 布局」**——图标随当前方案变化（**4 宫格＝完整布局** / **3 横线＝精简布局**），点击即**直接在完整与精简两方案间来回切换**，并弹出顶部浮层提示「已切换为精简布局 / 已切换为完整布局」。原先的 DIY 布局弹窗（方案卡片列表 + 确定按钮）整体下线，省去「点按钮 → 选方案 → 再确定」的多余动作。
- **导入导出精简**：从「导入导出」的模块清单中移除 **「DIY布局」**（隐藏模块设置）与 **「DIY布局模板」**（快速切换的布局方案）两项。布局已退化为一个纯本地的即时切换开关，不再参与备份；连带清理了版本表 `BACKUP_MODULE_VERSIONS`、导出装配、导入计数与导入应用四条分支。
- **非标准历史状态归一**：若用户当前处于历史遗留的自由勾选状态（不属于完整/精简任一方案），点一下按钮即视为「切到精简」并归一，保证任何状态下点一下都有确定结果，不出现「无目标可切」的悬空分支。
- **死代码清理**：`mergeLayoutTemplates()` 已删除——它唯一的调用方是「导入备份时合并布局模板」那条分支，该分支随选项一并下线。
- **存储层零改动**：`hiddenModules` / `layoutTemplates` 两个 localStorage key **与格式完全保留**（含 `pw_ultimate_` 前缀机制），`saveLayoutPrefs` / `saveLayoutTemplates` / `normalizeLayoutTemplates` / `applyLayoutVisibility` / `syncEarlyLayoutStyle` / `syncLayoutCompositeVisibility` 等持久化与防闪烁链路原样生效，老用户已有的布局状态继续有效，不会因本次改动丢数据。
- **触控体验**：按钮从「开弹窗」升级为「高频直接操作」，触控高度由 26px 提升至 **38px**（≤480px 屏为 36px），点按更稳；补齐 `:active` 按压反馈与 `:focus-visible` 键盘聚焦轮廓。360 / 390 / 414px 手机与平板、桌面下按钮高度、位置、无横向溢出均已实测通过。
- **无障碍**：`aria-label` 与 `title` 随状态动态更新为「当前：精简布局 · 点击切换为完整布局」这类明确描述，读屏用户可准确得知当前状态与点击后果；移除了已失效的 `aria-haspopup="dialog"`。

## 8.3.22（修复安卓模式按钮比例尾部省略号）
- **修复**：安卓端「礼物码单 / 单子码单」的分成比例后面多出一个省略号「…」，iOS 端正常。现已解决，两端显示一致。
- 根因（两层）：
  1. `.mode-btn__label` 用 `text-overflow: ellipsis` 裁剪「名称 + 空格 + 比例」这样的**三段拼接文本**。这种结构对字体度量与断行算法极其敏感——iOS WebKit 的度量较保守，Android WebView（各厂商内核版本差异很大）会把内容判定为溢出并在尾部加省略号，即使实际宽度完全放得下。
  2. `html.android-phone .mode-btn { font-size: 14px }` 的选择器权重高于全局 `@media (max-width: 420px) .mode-btn { font-size: 13.5px }` 与 `@media (max-width: 344px) { … 11.5px }`，导致**安卓的小屏字号降级完全失效**：窄屏下 iOS 已降到 13.5px/11.5px，安卓仍固定 14px，比 iOS 宽出一截，进一步逼近溢出边界。
- 修复方式：
  1. **彻底移除裁剪**：`.mode-btn__label` 去掉 `overflow: hidden` + `text-overflow: ellipsis` + `white-space: nowrap` 三件套，改为 `inline-flex` 行布局，名称与比例各自 `nowrap`、`flex: 0 0 auto`。文字从此**不可能**被截断成省略号，任何 WebView 都一样。`.mode-btn` 自身也一并移除 `text-overflow: ellipsis`（改为 `clip`），不做二次裁剪。
  2. **修正降级链**：安卓对应档位的降级规则移入 `html.android-phone` 区块内部（位置更靠后、与 `font-size: 14px` 同权重即可胜出），补齐 ≤420px → 13.5px、≤344px → 11.5px 两档，并同步收敛 label 右避让与图标尺寸。
- 修复后验证：安卓与 iOS 在同一视口下**字号完全一致**（320/344px = 11.5px，360/412px = 13.5px，≥421px 走默认）；320 → 1440px 共 15 档视口 × 7 个平台组合 × 6 种比例文本（含 `(888/888/888)` 这类超长自定义值）共 360 个检查点全部通过，无一处 `text-overflow: ellipsis` 残留、无一处内容被裁。
- 纯样式修复：订单计算、加价引擎、模式比例存储、DIY 布局等模块的业务逻辑与存储结构零改动。

## 8.3.21（整框触发切换比例 + 加价数字可选范围 + DIY布局视觉精修）
- 「单子码单 / 礼物码单」回归资料库（8.3.18）单行样式：按钮文字恢复为 `单子码单 (5/20/75)` 单行显示，不再拆成「名称 + 比例数字」两行。比例切换的小箭头图标缩小（15px，手机 ≤420px 再降至 13px）并贴到框内靠右边缘、透明度降到 0.34，只作提示、不与文字争视觉；图标不接收任何指针事件，触发区域为整个按钮框。窄屏（≤420px / ≤344px）分级下调字号与图标尺寸，保证 320px 宽下文字与图标仍不重叠、不溢出。
- 分成比例快捷切换改为「整框触发」：点击**当前已选中**的模式按钮 = 轮转该模式的分成比例（默认 → 自定义1 → 自定义2 → … → 默认）；点击**另一个**模式按钮 = 切换模式。触发区域由「22px 小圆点」扩大为「整个按钮框」，手机/平板下点按明显更容易。仅有一个档位时退化为「原地重算」并提示暂无其他比例，保留「点一下刷新计算」的既有习惯。
- 加价输入框的**纯数字**也纳入候选选择器：输入数字后同样弹出「全部项目 + 各项目」，默认高亮「全部项目」并在数字输入时实时预览；点选某个项目后，该数字加价只作用到该项目，行为与关键词候选完全一致。不主动选择时与旧行为（数字加价作用于全部项目）完全等价，无回归。
- 加价输入框行为矩阵（8.3.21 后）：空 → 不弹；**数字 → 弹候选**；关键词命中规则 → 弹候选；关键词未命中规则 → 不弹；礼物模式 → 不弹；仅一个项目 → 不弹（直接应用）。
- 修复：数字加价此前在引擎层硬编码作用于全部项目（`targetIndex` 未被读取），即使候选可选也不会真正定向；现已补上定向逻辑。
- DIY 布局弹窗视觉精修：方案卡片改为两栏结构（左文字 / 右选中标记），加大内边距与行高形成呼吸感；选中态同时给出「左侧 3px 主题色竖条 + 右侧圆形对勾」双重反馈，未选中时对勾淡出；卡片 hover 轻微上浮并加深描边，补齐 `:focus-visible` 键盘聚焦轮廓；确定按钮由小号提升为常规尺寸，说明文案重写并补全 `role="radiogroup"` / `aria-pressed` 无障碍属性。
- 底层零改动：`hiddenModules` / `layoutTemplates` 存储 key 与格式不变，旧数据继续生效；订单计算、加价引擎、老板记忆、价格库、提取、数据备份等模块未做任何逻辑调整。

### 8.3.21 代码审计（对全部改动逐行复核后追加的修复与清理）
- 修复（加价候选面板定位）：面板释放所有权时未复位 `position/top/left` 内联样式。当 service 或 price-quick 接管面板、或面板被隐藏后 `position:fixed` 残留，加价框再次唤起候选时会沿用上一次的过期坐标、出现在错误位置。现统一在 `releaseSuggestionSurfaceState()` 内复位定位。
- 修复（加价候选误收面板）：`hideSharedSuggestions()` 原先仅在 `force` 为真或自身持有面板时才真正收起，导致加价框在失去所有权后仍可能残留显示旧内容。去掉该分支，改为「失去所有权即收起」，同时移除已成死参数的 `force`。
- 修复（加价候选误取消）：点击候选列表中的**当前选中项**原先会取消选择并立即收起面板、使加价范围静默回落到「全部项目」。对「纯数字加价」这种默认就作用于全部项目的场景属于误操作陷阱，现改为**重复点击保持选择**（只重算，不取消）。
- 修复（候选面板被顶出视口）：加价框位于视口下缘时，面板按「贴框下方」定位会被推到视口外不可见（小屏、键盘顶起视口时尤其明显）。现将纵向定位改为**可翻转**：下方放不下则贴框上方，仍放不下则贴视口底部内缩 8px。该缺陷为 8.3.19 引入候选面板时的既有行为，本次一并修正。
- 清理（冗余代码）：`ModeFlowFeature` 中删除未被任何位置调用的 `handleModeButtonClick()` 桥接方法与 `ratioFeature` getter，模式按钮事件改为直接委托 `app.handleModeButtonClick`，避免同一份分支逻辑在两个类里各维护一份。
- 审计结论：本次改动共 56 个变更块（新增 232 行 / 删除 113 行），逐项复核后确认无死代码、无孤儿 CSS 选择器、无重复事件绑定、无未使用变量；所有新增 CSS 选择器均有 HTML/JS 引用闭环；`.mode-btn`、`.layout-scheme-item` 等看似重复的规则均位于媒体查询内，属有意的响应式覆盖；12 个 localStorage key 出现次数与基线完全一致，本地数据无迁移风险。

## 8.3.20（录单界面微调 + DIY 布局固定方案）
- 加价输入框提示改为折数同款两行内联提示：第一行「直接输入」、第二行「关键词/数字」（10.5px 居中、浅色，输入后隐藏、清空后恢复）；原单行 placeholder 文案退役。
- DIY 布局简化为两个固定方案：方案一「完整布局」显示全部内容；方案二「精简布局」隐藏「自定义比例按钮」和「服务时长输入框」。原 28 项自由勾选列表与自定义模板界面下线；底层 hiddenModules 存储 key 与格式不变，历史数据继续生效，旧的自定义隐藏状态打开弹窗时不会误选方案；模板数据保留在本地但不再展示。
- 「单子码单 / 礼物码单」按钮之间恢复 1px 竖条分割线（与容器边框同色），平板三列与手机 2×2 布局均生效。
- 备注输入框提示字号对齐折数内联提示（10.5px），手机/平板、安卓/iOS 显示一致。
- 附带修复：DIY 布局弹窗从固定 80vh 高改为内容自适应，消除内容减少后的底部大空白。

## 8.3.19
- 快捷切换分成比例：单子/礼物模式按钮内新增简约双向箭头图标，单击即按「默认 → 自定义1 → 自定义2 → … → 默认」顺序轮转已保存的分成比例，无需再进自定义比例弹窗逐个切换。仅有默认比例时图标自动置灰，不给出无意义的可点击暗示。
- 加价候选选择：加价输入框从「必须手写 关键词@项目名 语法」升级为「输入即预览、点击即选择」。输入关键词且服务类型含多个项目时自动弹出候选（默认高亮「全部项目」），点击即锁定目标，预览实时显示；再次点击同一项则取消选择并提示「请选择加价范围」。填入数字仍直接作用于全部项目；单个项目时默认直接应用、不弹候选。
- 加价交互细节：候选面板贴合加价输入框显示并跟随滚动与窗口尺寸变化；输入法组合输入期间不干扰候选；纯数字与未命中规则时不弹面板；礼物模式不参与加价，行为保持不变。
- 模式按钮两行布局：单子/礼物按钮改为「名称 + 比例数字」两行显示，修复加入切换图标后「礼物码单 (10/20/7…)」被截断的问题；激活态渐变文字效果同步迁移到两行文字，切换图标在激活按钮上以主题色显示。
- 备注框回归纯备注：备注不再触发加价（原「在备注写关键词/@关键词加价」的用法请改用加价输入框，功能完全覆盖且更强：数字加价、关键词候选点选、@项目名/@全部 写法均保留），备注框提示语同步更新，加价框新增「数字直接加价，或输入加价关键词」提示；订单输出中备注原样保留、不再做关键词替换。
- 兼容性：手写「关键词@项目名」「关键词@全部」等既有写法（在加价框内）完全保留且优先级最高，加价引擎未指定目标时的报错行为与 8.3.18 一致。订单计算、老板记忆、价格库、提取、数据备份等模块零改动。

## 8.3.18（工程整理，无功能变化）
- 控制台噪声清理：启动时不再出现「缺失 DOM 元素: autoUnitPrice, apModeRound, apModeHour, autoPriceQuickList, autoPriceQuickPanel, dcFillSection, dcFillOptions」假错误。这些元素本就在初始化之后由 AutoPriceFeature 与懒加载的时长计算器动态写入，属预期行为，现将其登记为延迟注入元素、不计入缺失诊断。
- 仓库清理：移除 Trae IDE 遗留的 `.trae-html-share-packages/`（内含 8.3.14 旧快照，已落后 3 个版本，无任何代码引用）。
- 文档修正：README「项目结构」补全为仓库实际文件树；修正「无埋点、无远程请求」的失实表述，明确业务数据不上传、另有不含业务数据的匿名使用统计；`.gitignore` 移除与事实矛盾的 `madan-*.zip` 规则并注明更新包必须留在仓库（Pages 直出，供 OTA 下载）。
- 本次为纯工程整理：不改任何业务逻辑、UI、存储 key 与数据结构，订单计算、老板记忆、价格库、提取等行为与 8.3.17 完全一致。
- 测试资产：~~新增 `check-structure.js` 静态结构门禁，以及~~ `tests/` 目录下的全链路测试脚本（8.3 架构版，适配懒加载 chunk，含提取边界哨兵）、DOM 全链路与组合联动测试。上游原版测试脚本保留归档。
  > **更正（2026-09-14）**：本条原写「新增 `check-structure.js` 静态结构门禁」，但该文件**从未实际提交到仓库**。验证方式：
  > ```
  > $ git ls-files | grep check-structure     # 无输出
  > $ git log --all --diff-filter=A -- check-structure.js   # 无输出（全历史从未新增过）
  > ```
  > 全历史路径名中仅 `update-checker.js` 含 "check"，可排除「曾被提交后又删除」的可能。
  > 此处按事实更正，避免后续维护者按不存在的文件排查问题。若将来确需「静态结构门禁」能力，
  > 应新建并以独立变更条目记录，而非沿用本条描述。

## 8.3.17
- 主页「候选单价名片」快捷网格（¥35鹅鸭杀/¥17技术匹配 那一排）不再展示：与单击服务类型输入框弹出的候选名片功能重合，后者更符合使用直觉，保留后者。
- 仅隐藏视觉，所有底层逻辑（数据计算、单价/局数/小时切换、服务类型候选名片、记忆库联动）原样保留；DIY 布局里该模块开关同步移除。

## 8.3.16
- 老板记忆库新增「最近老板」分类页：与「老板记忆」并列切换，可查看名字/派单/折数/使用次数/最近使用时间，并支持编辑、删除、⭐一键固化到老板记忆库（已存时实心高亮，点击同步最新折数）。
- 老板记忆库新增搜索框（跟随当前页签，支持拼音/首字母/模糊），搜索过滤后列表项按 key 定位，不会再删错条目。
- 折数冲突自动合并：同一老板+派单视为同一人，码单时折数以最新一次为准，不再在「最近老板」里产生两条不同折数的候选；旧的重复记录下次打开自动合并，无需手动清理。
- 使用满 2 次自动写入老板记忆库的折数同步升级为最新折数，错误折数不再被固化。
- 编辑老板记录时表单新增「取消」按钮，最近老板页签未编辑时表单自动收起，界面更清爽。

## 8.3.15
- 定制/建议联系：弹窗内微信按钮点击后复制微信号并自动唤起微信（复用生成订单后的唤起链路，微信内置浏览器中不重复跳转），无需再手动切换。
- 小标题改为「定制/建议👉」，联系按钮文案改为「联系方式」（保留原信息图标）。

## 8.3.14
- DIY布局即时生效：修改布局并确定后当前页面立即应用，不再需要重启。
- 名字与学习管理：分类栏（已确认名字/学习映射/忽略名单）居中显示；忽略名单已并入「数据备份 → 名字修复学习」模块，可正常导出与导入（覆盖/合并均支持）。
- 生成条件高亮：满足常规生成条件（总价有效、已填折扣合法）时，「生成并复制」按钮（含悬浮栏按钮）呼吸光晕提示就绪状态。
- 订单详情实时生成：输入信息变化即实时计算并刷新订单详情预览，无需点击生成；正在编辑折扣输入时自动避让，不干扰输入。
- 定制/建议联系：小标题改为「定制/建议联系」，新增联系按钮，弹窗内可选微信（复制微信号 srhovo）或 QQ（🐧 跳转加好友/进群）两种联系方式，图标使用内联 SVG，加载零请求。

## 8.3.13
- 数据损坏恢复提示静默化：取消「检测到本地数据已损坏，已尝试从其他备份恢复」提示弹窗；损坏检测、cookie 备份恢复降级链与控制台日志全部保留，仅去除视觉打扰。

## 8.3.12
- 热更新静默化：移除"发现新版本"提示弹窗，改为启动后自动检测、后台下载并切换，全程无感、无需点击。
- 更新失败仅记录控制台日志并于下次启动自动重试；notifyAppReady 超时自动回退机制保持不变，localStorage 用户数据全程不受影响。

## 8.3.11
- 数据安全加固：本地存储数据损坏时自动从 cookie 备份恢复并提示，不再静默覆盖；多标签页同时使用时数据自动同步，不再互相覆盖；cookie 写入失败时提示更准确。
- 名字提取修复：修正映射优先级高于名字匹配；带 emoji 装饰的名字不再被截断；长名字包含已确认短名字时不再被短名吞掉。
- 确认弹窗"说到做到"：弹窗里删除的名字进入忽略名单，不再反复出现在提取结果；新增学习映射管理页签（查看/删除自动修正规则）。
- 键盘与校验：修复方向键跳项；手机拼音输入不再被候选列表打断；校验失败自动滚动定位到缺失项。
- 删除更安全：历史记录删除改为 10 秒内可撤销（Toast 撤销按钮）。
- 版本显示：同步标题版本号（此前停留在 8.3.4）。

## 8.3.10
- 备注输入框高度压为单行（44px），与右侧加价输入框完全等高对齐（桌面与手机视口实测一致）。
- 备注文字自适应：输入长文字自动缩小字号（15px→最低10px）确保完整显示，删短或清空后字号自动恢复；两行占位提示同步缩小字号完整显示。
- 删除加价输入框内的占位提示文字「如15或关键词」。
- 技术细节：备注框 font-size 不再参与全局 transition 动画（避免自适应缩放测量读到过渡中间值）；空值时占位文字高度不计入溢出判定。

## 8.3.9
- 陪玩提取「已保存名字」弹窗新增手动添加：粘贴文本识别不出陪玩名字时，可先手动把名字加入已保存列表，再粘贴即可正常提取；列表项会标注「手动添加」来源。
- 备注输入框旁横向并列新增「加价」输入框：输入纯数字（含小数、全角）按每个项目自身结算单位（局/小时）整体加价；输入关键词同样可触发加价规则。
- 输出模版优化：备注区的关键词自动替换为对应加价规则的标准名称（触发词求简便、名称更完整）；加价输入内容追加在备注后并换行分隔，数字输入输出为「加价15」样式。
- 加价输入已接入历史记录保存/回填、全部清除、草稿快照签名与实时预览失效链路。

## 8.3.4
- 当前稳定版本。
- 数据备份 UI 整体焕新：导入区新增「选择备份文件」与「选择 Club」双入口，适配手机 / 平板双端。
- 新增「选择 Club」功能：从 GitHub 记忆库仓库拉取 Club 清单并加载对应 JSON 备份，自动填入导入框并触发预览。
- 导入框支持粘贴 / 选择文件 / 选择 Club 后自动实时解析预览，减少操作步骤。
- 修复标题版本号未同步问题。
- Club 文件 URL 增加 encodeURIComponent，提升中文文件名兼容性。

## 8.3.1
- 历史稳定版本。

## 8.4.0-candidate（候选）
- 单价记忆库信息流调整：拆掉"高级规则与礼物"的 details 包裹，区间价格规则 / 备注触发加价规则 / 礼物名称记忆改为直接可见、可独立展开，减少一级点击。
- 三者互相独立、互不影响；管理价格库 / 新增或编辑单价的互斥手风琴行为不变。
- 基于 8.2.26，不含 8.3.0 候选版的配色改动。

## 8.2.26
- 热修：修复手机宽度下"备注触发加价规则"与"礼物名称记忆"表单溢出裁切、部分字段不可见的问题。
- 仅新增一条 CSS 规则，不改其他 UI、算法与数据结构。

## 8.2.25
- 统一"时长计算 / 提取名字"的按钮高度、文字字号、图标尺寸和垂直基线。
- 保留手机、平板与桌面端原有布局及全部业务逻辑。

## 8.2.23
- 修复结构化项目中间精度过早取整。
- 结算量持久化保留 6 位，最终项目金额保留 3 位，并同步修正结构化历史派生结果。
